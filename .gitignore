# Secrets / API keys
polymarket.keys*
.env

# Local capture runtime files (datasets under data/ are versioned)
data/live/rfq_capture.db
data/live/rfq_raw.jsonl
data/live/combo_markets.json.gz
data/live/*.log
data/live/*.lock
data/live/*.tmp
data/live/*-wal
data/live/*-shm

# NFL pipeline: backtest outputs are regenerated locally
results/
params/.staging/
# Walk-forward weekly params for the simulated RFQ datasets: one file per
# (season, week), regenerated by scripts/gen_nfl_rfq_dataset.py.
params/history/

# Python
__pycache__/
.pytest_cache/

# Backtest run artifacts (reproducible via scripts/backtest.py; never commit)
runs/
